#include <vector>
#include <algorithm>
class Solution {
public:
    int removeElement(std::vector<int>& nums, int val) {
        //const auto colvo = nums.erase(nums, val);
        return nums.size();
    }
};